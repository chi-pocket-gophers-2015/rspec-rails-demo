<h1>Welcome <%= current_user.username %></h1>

  <h3>Current Articles</h3>
    <% current_user.articles.each do |article| %>
      <p><%= link_to article.title, category_article_path(article.category, article) %></p>
    <% end %>
