<% @categories.each do |category| %>
<div class="col-md-4">
  <div class="panel panel-default">
    <p class="panel-body"><%= link_to category.name, category_articles_path(category) %></p>
  </div>
</div>
<% end %>
